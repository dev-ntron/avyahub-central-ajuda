# Configurações do Banco de Dados
DB_HOST=localhost
DB_NAME=avyahub_help
DB_USER=seu_usuario_db
DB_PASS=sua_senha_db

# BASE_PATH - OBRIGATÓRIO!
# Para subpasta: /ajuda
# Para raiz: /
# Para subdomínio: /
BASE_PATH=/ajuda

# Configurações Gerais
SITE_URL=/
UPLOADS_DIR=uploads/
MAX_UPLOAD_SIZE=5242880

# Configurações de Ambiente
# development | production
APP_ENV=development
APP_DEBUG=true

# Timezone (https://www.php.net/manual/en/timezones.php)
APP_TIMEZONE=America/Sao_Paulo

# Configurações de Cache
ENABLE_CACHE=true
CACHE_DURATION=3600

# Chave de segurança para sessões
# GERE UMA CHAVE ÚNICA usando: openssl rand -hex 32
APP_SECRET_KEY=ALTERAR_ESTA_CHAVE_SECRETA_EM_PRODUCAO

# OBSERVAÇÕES IMPORTANTES:
# 1. Renomeie este arquivo para .env após configurar
# 2. Configure BASE_PATH corretamente para sua estrutura de pastas
# 3. NUNCA commite o arquivo .env no Git
# 4. Em produção, use APP_ENV=production e APP_DEBUG=false
# 5. O usuário admin é criado durante a instalação web